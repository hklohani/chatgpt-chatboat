// Service module

//create service

request = {
  service_type: ['express', 'regular'],
  service_description,
  service_price,
  service_product_id,
  service_date,
  service_status,
};
response = {
  status,
};
